# account-statement-generator
Production-grade reactive service for monthly account statement generation from ledger entries with immutable outputs, audit metadata, checksums, idempotent reruns, download APIs, Kafka outbox events, and Redis-backed coordination.
